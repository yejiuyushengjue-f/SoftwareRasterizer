# Agent 工作指南

## 最高优先级

1. 任何 Agent 进入本仓库后，先读本文件 `AGENT.md`，再读 `README.md`、`CMakeLists.txt` 和相关构建/测试文件。
2. `AGENT.md` 与 `docs/resume-roadmap.md` 是本地协作说明文件，当前为 git 未跟踪文件；不要把它们提交到 GitHub，除非用户明确改变要求。
3. 只修改任务要求范围内的文件。仓库中可能有其他工作者的未提交或未跟踪改动，不要回退、删除或覆盖他人工作。

## 协作与提交注意事项

- 修改前后检查：
  ```powershell
  git status --short
  ```
- 不要随意 stage、commit、push 未跟踪文件或与任务无关的文件。
- 如果遇到出乎意料的构建、编码、工具链或协作问题，先告知用户；若用户要求或问题会影响后续 Agent，可把简洁的排查方法补充到本文件，除此以外不允许随意向该文件写入内容。

## README.md 编码注意事项

- 已验证 `README.md` 当前为有效 UTF-8，且无 UTF-8 BOM。
- 在 Windows PowerShell 下，直接 `Get-Content README.md` 可能按系统默认编码读取，中文会显示为乱码。
- 读取 README 时优先显式使用 UTF-8：
  ```powershell
  Get-Content -LiteralPath README.md -Encoding UTF8
  ```
- 如果编辑器或终端中看到乱码，先用 UTF-8 重新打开或重新读取；不要用默认 ANSI/系统编码保存覆盖 `README.md`。
- 如需排查编码，可检查文件开头字节或用严格 UTF-8 解码验证；若不能复现问题，在说明中写成“已知注意事项/排查方法”，不要臆造结论。

## 正式文档写作规范

- 正式项目文档必须使用稳定、可复现、与执行环境无关的表述，不允许写入只服务于本次 Agent 操作的说明。
- 禁止在正式文档中出现临时路径、临时 worktree、临时 build 目录、`C:\tmp\...`、worker 编号、一次性目录名等环境痕迹。
- 禁止在正式文档中出现临时函数、临时开关、临时 benchmark harness、调试草稿、内部执行过程、临时验证脚本或其他一次性实现细节。
- 禁止在正式文档中记录“某个 worker/某次操作/某个临时目录下运行成功”之类仅对本次执行成立的说明。
- 如需保留验证方法、测量方式或操作步骤，应转写为仓库内可长期复用的命令、正式入口、固定配置或环境无关的说明。
- 如需记录验证结果，可写成“已使用 Release 测试套件验证并通过”这类稳定结论；不要写入临时目录、临时命令上下文或仅一次执行可见的内部过程。
